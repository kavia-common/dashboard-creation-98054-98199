{
  "name": "react-kavia",
  "version": "0.2.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.6.8",
    "chart.js": "^4.4.1",
    "jwt-decode": "^4.0.0",
    "react": "^18.2.0",
    "react-chartjs-2": "^5.2.0",
    "react-dom": "^18.2.0",
    "react-hook-form": "^7.51.4",
    "react-router-dom": "^6.22.3",
    "react-scripts": "^5.0.1",
    "recharts": "^2.10.3"
  },
  "scripts": {
    "start": "npx react-scripts start",
    "start:https": "HTTPS=true npx react-scripts start",
    "build": "npx react-scripts build",
    "test": "cross-env CI=true npx react-scripts test --watchAll=false",
    "lint": "eslint --ext .js,.jsx src",
    "eject": "npx react-scripts eject",
    "serve:prod": "npx serve -s build -l 3000",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test:e2e": "start-server-and-test start http://localhost:3000 cy:run"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "cypress": "^13.7.3",
    "@testing-library/cypress": "^10.0.2",
    "start-server-and-test": "^2.0.7"
  }
}
